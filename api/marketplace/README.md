# Society Services Marketplace API (scaffold)

This module is scaffolded with REST endpoints, controllers, middleware, validation and error handling.

Current task delivered:
- Frontend UI module pages + styles + UI logic (demo/local storage).

Next integration step:
- Implement backend routes/controllers under `api/services-marketplace/*` (recommended).
- Mount in `server.js` under `/api/marketplace`.

Supabase SQL schema is generated separately:
- `supabase/schema_society_services_marketplace.sql`

